import { sx_UnderLineNavButton } from '../styles/underlineSx';
import { Divider } from './mui';

function UnderLineNavButton() {
    return <Divider sx={sx_UnderLineNavButton} />;
}

export default UnderLineNavButton;
